# config/settings.py
# "OMNI Trading System의 전체 설정을 관리하는 환경 설정 파일"

import os
from dotenv import load_dotenv

load_dotenv()

class Settings:
   UPBIT_ACCESS_KEY = os.getenv('UPBIT_ACCESS_KEY')
   UPBIT_SECRET_KEY = os.getenv('UPBIT_SECRET_KEY') 
   OPENAI_API_KEY = os.getenv('OPENAI_API_KEY')
   
   # =========================================================================
   # 🤖 GPT-5 설정
   # =========================================================================
   GPT_MODEL = 'gpt-5'  # GPT-5 사용
   GPT_TEMPERATURE = 0.1  # 일관성 있는 응답
   
   # =========================================================================
   # 💰 거래 기본 설정
   # =========================================================================
   AVAILABLE_BALANCE_RATIO = 0.97  # 사용 가능 잔고 비율 (97%)
   ENTRY_WAIT_TIMEOUT_HOURS = 1.0  # 진입가 대기 타임아웃 (1시간)
   
   # =========================================================================
   # 📊 동적 포지션 사이징 (시장 추세 기반)
   # =========================================================================
   # 시장 추세별 포지션 크기 (전체 자산 대비)
   POSITION_SIZE_BULL = 0.97      # 상승장: 97% (전체 자산의 97%)
   POSITION_SIZE_SIDEWAYS = 0.75  # 횡보장: 75% (전체 자산의 75%)
   POSITION_SIZE_BEAR = 0.30      # 하락장: 30% (전체 자산의 30%)
   
   # 포지션 크기 한계
   MIN_POSITION_SIZE = 0.10  # 최소 10%
   MAX_POSITION_SIZE = 0.97  # 최대 97%
   
   # 로깅 설정
   POSITION_SIZE_LOGGING = True  # 포지션 크기 계산 로깅
   
   # =========================================================================
   # 🎯 수익 목표 및 리스크 관리
   # =========================================================================
   # 목표 수익률 범위 (GPT-5 참고용)
   TARGET_PROFIT_MIN = 3     # 최소 3%
   TARGET_PROFIT_MAX = 12    # 최대 12%
   TARGET_PROFIT_PREFERRED = 6  # 선호 6%
   
   # 손절 기준 (GPT-5 참고용)
   STOP_LOSS_RATE = -2.5  # 최대 손실 -2.5%
   
   # =========================================================================
   # 📈 API 호출 제한
   # =========================================================================
   # 업비트 API 제한 (분당 100회, 초당 10회)
   UPBIT_REQUESTS_PER_MINUTE = 90  # 여유분 확보
   UPBIT_REQUESTS_PER_SECOND = 8   # 여유분 확보
   
   # =========================================================================
   # 💾 데이터 저장 경로
   # =========================================================================
   DB_PATH = 'logs/trade_history.db'
   PRINCIPLES_FILE = 'memory/Principles.md'
   
   # =========================================================================
   # 🧠 학습 시스템 설정
   # =========================================================================
   # 원칙 업데이트 설정
   PRINCIPLES_UPDATE_IMMEDIATELY = True  # 매 거래 후 즉시 원칙 업데이트
   MIN_TRADES_FOR_PRINCIPLES = 1  # 최소 1개 거래부터 원칙 생성
   
   # =========================================================================
   # 📊 로깅 및 디버깅
   # =========================================================================
   DEBUG_MODE = False  # 디버그 모드
   LOG_LEVEL = 'INFO'  # 로그 레벨: DEBUG, INFO, WARNING, ERROR
   
   # 추세 변화 알림
   TREND_CHANGE_LOGGING = True  # 시장 추세 변화 로깅
   
   # =========================================================================
   # ⚙️ 시스템 동작 설정
   # =========================================================================

   # 거래 복구
   AUTO_RECOVER_ON_RESTART = True  # 프로그램 재시작 시 활성 거래 자동 복구

settings = Settings()